# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 数据库（异步）
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
alembic>=1.14.0

# 配置与校验
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.0.0  # pydantic EmailStr 依赖

# 认证与安全
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0  # 直接使用 bcrypt（passlib 1.7.4 与 bcrypt 5.x/Python 3.13+ 不兼容）
cryptography>=43.0.0

# HTTP 客户端（调用 CF API）
httpx>=0.27.0

# 前端（Jinja2 模板 / 表单解析 / 会话签名）
jinja2>=3.1.0
python-multipart>=0.0.9
itsdangerous>=2.2.0

# 时区数据（Windows 缺少 IANA tz 数据库，alembic UTC 时间戳依赖）
tzdata>=2024.1

# 测试
pytest>=8.3.0
pytest-asyncio>=0.24.0
